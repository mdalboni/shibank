name: shibank
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get:
  flutter_spinkit:
  unicorndial:
  animated_widgets:
  faker:
  keyboard_visibility:
  curved_navigation_bar:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/